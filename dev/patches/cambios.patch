diff --git a/dev/dev.html b/dev/dev.html
index 17538a42eb1e9b1d2d88766a80e05b88a9478105..4125c758fecedbe1283296d3eabdc9c0c25a2d93 100644
--- a/dev/dev.html
+++ b/dev/dev.html
@@ -595,68 +595,89 @@ body{
 
   <!-- ENCABEZADO -->
   <div class="header">
     <h1>🏭 Showroom IT/OT</h1>
   </div>
 
   <!-- STEP 1: FORMULARIO -->
   <div class="form-panel step-panel active" id="step-1">
     <h2>Paso 1 de 3 · Formulario de visita</h2>
 
     <h3 style="margin-bottom:12px;color:#ff6b35;">A) Logística de la visita</h3>
     <div class="form-group">
       <label for="visitType">Tipo de visita (Presencial / Virtual)</label>
       <select id="visitType">
         <option value="">Selecciona una opción</option>
         <option value="Physical">Presencial</option>
         <option value="Virtual">Virtual</option>
       </select>
     </div>
     <div class="form-group">
       <label for="visitDate">Fecha de visita *</label>
       <input type="date" id="visitDate" required>
       <span class="error-message" id="error-visitDate"></span>
     </div>
     <div class="form-group">
-      <label for="visitTime">Hora de visita *</label>
-      <input type="time" id="visitTime" required>
+      <label for="visitTime">Hora (intervalos de 30 min) *</label>
+      <select id="visitTime" required>
+        <option value="">Selecciona una hora</option>
+        <option value="9:00 AM">9:00 AM</option>
+        <option value="9:30 AM">9:30 AM</option>
+        <option value="10:00 AM">10:00 AM</option>
+        <option value="10:30 AM">10:30 AM</option>
+        <option value="11:00 AM">11:00 AM</option>
+        <option value="11:30 AM">11:30 AM</option>
+        <option value="12:00 PM">12:00 PM</option>
+        <option value="12:30 PM">12:30 PM</option>
+        <option value="1:00 PM">1:00 PM</option>
+        <option value="1:30 PM">1:30 PM</option>
+        <option value="2:00 PM">2:00 PM</option>
+        <option value="2:30 PM">2:30 PM</option>
+        <option value="3:00 PM">3:00 PM</option>
+        <option value="3:30 PM">3:30 PM</option>
+        <option value="4:00 PM">4:00 PM</option>
+        <option value="4:30 PM">4:30 PM</option>
+        <option value="5:00 PM">5:00 PM</option>
+        <option value="5:30 PM">5:30 PM</option>
+        <option value="6:00 PM">6:00 PM</option>
+      </select>
       <span class="error-message" id="error-visitTime"></span>
     </div>
     <div class="form-group">
       <label for="presentationLanguage">Idioma de presentación (Español / Inglés)</label>
       <select id="presentationLanguage">
         <option value="">Selecciona una opción</option>
         <option value="Spanish">Español</option>
         <option value="English">Inglés</option>
       </select>
     </div>
     <div class="form-group">
       <label for="foodOption">Alimentos *</label>
       <select id="foodOption" required>
         <option value="">Selecciona una opción</option>
         <option value="Coffee break">Coffee break</option>
-        <option value="Box lunch">Box lunch</option>
+        <option value="Comida en caja">Comida en caja</option>
         <option value="Nada">Nada</option>
         <option value="Personalizada">Personalizada</option>
       </select>
       <span class="error-message" id="error-foodOption"></span>
       <p class="form-note">Nota: Considerar 4 personas por parte del showroom.</p>
     </div>
     <div class="form-group" id="foodDetailGroup" style="display:none;">
       <label for="foodDetail">Detalle (opción personalizada) *</label>
       <input type="text" id="foodDetail" placeholder="Especifica la opción de alimentos…" disabled>
       <span class="error-message" id="error-foodDetail"></span>
     </div>
     <div class="form-group" id="costCenterGroup">
       <label for="costCenter">Centro de costos *</label>
       <input type="text" id="costCenter" placeholder="Centro de costos" disabled>
       <span class="error-message" id="error-costCenter"></span>
     </div>
 
     <h3 style="margin:20px 0 12px;color:#ff6b35;">B) Datos del cliente</h3>
     <div class="form-group">
       <label for="companyName">Empresa (cliente) *</label>
       <input type="text" id="companyName" placeholder="Ej: ACME Manufacturing" required>
       <span class="error-message" id="error-companyName"></span>
     </div>
     <div class="form-group">
       <label for="sector">Sector / Industria</label>
@@ -1008,69 +1029,93 @@ function toggleCostCenter(){
 
 function setError(fieldId, message){
   const error = document.getElementById(`error-${fieldId}`);
   if(error){
     error.textContent = message;
   }
 }
 
 function clearErrors(){
   document.querySelectorAll('.error-message').forEach(el=>{el.textContent='';});
 }
 
 function validateStep1(){
   clearErrors();
   let isValid = true;
 
   REQUIRED_FIELDS.forEach(field=>{
     const input = document.getElementById(field.id);
     if(!input) return;
     if(!input.value || !input.value.toString().trim()){
       setError(field.id, 'Este campo es obligatorio.');
       isValid = false;
     }
   });
 
+  if(!validateVisitDate({resetIfInvalid:true})){
+    isValid = false;
+  }
+
   const foodOption = document.getElementById('foodOption');
   const foodDetail = document.getElementById('foodDetail');
   const costCenter = document.getElementById('costCenter');
   if(foodOption && foodOption.value === 'Personalizada'){
     if(!foodDetail || !foodDetail.value.trim()){
       setError('foodDetail', 'Este campo es obligatorio.');
       isValid = false;
     }
   }
   if(foodOption && foodOption.value && foodOption.value !== 'Nada'){
     if(!costCenter || !costCenter.value.trim()){
       setError('costCenter', 'El centro de costos es obligatorio cuando hay alimentos.');
       isValid = false;
     }
   }
 
   return isValid;
 }
 
+function isWeekend(dateString){
+  const date = new Date(`${dateString}T00:00:00`);
+  if(Number.isNaN(date.getTime())) return false;
+  const day = date.getDay();
+  return day === 0 || day === 6;
+}
+
+function validateVisitDate({resetIfInvalid = false} = {}){
+  const visitDate = document.getElementById('visitDate');
+  if(!visitDate || !visitDate.value) return true;
+  if(isWeekend(visitDate.value)){
+    setError('visitDate','Selecciona un día hábil (lunes a viernes).');
+    if(resetIfInvalid){
+      visitDate.value = '';
+    }
+    return false;
+  }
+  return true;
+}
+
 function createAttendeeRow({withEmail = false} = {}){
   const row = document.createElement('div');
   row.className = 'attendee-row';
 
   const nameInput = document.createElement('input');
   nameInput.type = 'text';
   nameInput.placeholder = 'Nombre';
 
   const roleInput = document.createElement('input');
   roleInput.type = 'text';
   roleInput.placeholder = 'Rol / Puesto';
 
   row.appendChild(nameInput);
   row.appendChild(roleInput);
 
   let emailInput = null;
   if(withEmail){
     emailInput = document.createElement('input');
     emailInput.type = 'email';
     emailInput.placeholder = 'Correo electrónico';
     row.appendChild(emailInput);
   }
 
   const removeBtn = document.createElement('button');
   removeBtn.type = 'button';
@@ -1272,50 +1317,58 @@ function buildMailto(){
   body += `Asistentes de Scitum/Telmex:\n`;
   if(data.internalAttendees.length){
     data.internalAttendees.forEach((attendee, idx)=>{
       body += `- ${idx+1}. ${formatValue(attendee.name)} / ${formatValue(attendee.role)}\n`;
     });
   }else{
     body += `- ${EMPTY_VALUE}\n`;
   }
   body += `\n`;
 
   body += `E) Contexto estratégico\n`;
   body += `- Objetivo de la visita: ${formatValue(data.visitObjective)}\n`;
   body += `- Descripción del cliente, procesos críticos y operación: ${formatValue(data.customerDescription)}\n`;
   body += `- Problema principal a resolver: ${formatValue(data.mainProblem)}\n`;
   body += `- Nivel de madurez en ciberseguridad: ${formatValue(formatChoice(data.maturityLevel, DISPLAY_MAPS.maturityLevel))}\n\n`;
 
   body += `F) Diagrama / Soluciones seleccionadas\n`;
   if(solutions.length){
     solutions.forEach((solution, idx)=>{
       body += `- ${idx+1}. ${solution.name} — ${formatValue(solution.brand)}\n`;
     });
   }else{
     body += `- ${EMPTY_VALUE}\n`;
   }
 
+  body += `\nNotas operativas\n`;
+  body += `- Aforo máximo: 12 personas.\n`;
+  body += `- Alimentos: Considerar 4 personas por parte del showroom.\n`;
+  body += `- Horario operativo: Reservar 2 horas adicionales previas al evento para la preparación de la maqueta y 30 minutos posteriores para limpieza y restablecer la maqueta.\n`;
+  if(data.foodOption && data.foodOption !== 'Nada'){
+    body += `- Nota de alimentos: Considerar comida en caja / Coffee break / opción personalizada para el equipo del showroom, según corresponda.\n`;
+  }
+
   return {to: mailtoRecipient, subject, body};
 }
 
 function sendEmail(){
   const mailData = buildMailto();
   if(!mailData) return;
   const mailtoLink = `mailto:${encodeURIComponent(mailData.to)}?subject=${encodeURIComponent(mailData.subject)}&body=${encodeURIComponent(mailData.body)}`;
   if(mailtoLink.length>1900){
     document.getElementById('fallback-to').textContent = mailData.to;
     document.getElementById('fallback-subject').textContent = mailData.subject;
     document.getElementById('fallback-body').value = mailData.body;
     document.getElementById('fallback').style.display='block';
   }else{
     document.getElementById('fallback').style.display='none';
     window.location.href = mailtoLink;
   }
 }
 
 const LAYER_HEIGHT = 130;
 const purdueLayers = [
   {level:5, label:'', height:Math.round(LAYER_HEIGHT*1.3)},
   {level:4, label:'Nivel 4 – Red Empresarial', height:LAYER_HEIGHT},
   {level:3.5, label:'Nivel 3.5 – DMZ IT/OT', height:LAYER_HEIGHT},
   {level:3, label:'Nivel 3 – Operaciones del Sitio', height:LAYER_HEIGHT},
   {level:2, label:'Nivel 2 – Control de Supervisión', height:LAYER_HEIGHT},
@@ -2624,50 +2677,63 @@ function toggleBrands(solutionId){
     if(radios.length > 0){
       const anyChecked = Array.from(radios).some(r => r.checked);
       if(!anyChecked){
         radios[0].checked = true;
       }
     }
   }else{
     if(brandsGroup) brandsGroup.classList.remove('active');
     radios.forEach(r=>r.checked=false);
   }
   updateDiagram();
 }
 
 /* Init */
 document.addEventListener('DOMContentLoaded',()=>{
   updateDiagram();
   toggleCostCenter();
   addAttendee('clientAttendeesList', true);
   addAttendee('internalAttendeesList', false);
 
   const foodOption = document.getElementById('foodOption');
   if(foodOption){
     foodOption.addEventListener('change', toggleCostCenter);
   }
 
+  const visitDate = document.getElementById('visitDate');
+  if(visitDate){
+    visitDate.addEventListener('change',()=>{
+      if(!visitDate.value){
+        setError('visitDate','');
+        return;
+      }
+      if(validateVisitDate({resetIfInvalid:true})){
+        setError('visitDate','');
+      }
+    });
+  }
+
   const addClient = document.getElementById('addClientAttendee');
   if(addClient){
     addClient.addEventListener('click',()=>addAttendee('clientAttendeesList', true));
   }
   const addInternal = document.getElementById('addInternalAttendee');
   if(addInternal){
     addInternal.addEventListener('click',()=>addAttendee('internalAttendeesList', false));
   }
 
   const toStep2 = document.getElementById('toStep2');
   if(toStep2){
     toStep2.addEventListener('click',()=>{
       if(validateStep1()){
         showStep('step-2');
       }else{
         alert('Por favor completa los campos obligatorios.');
       }
     });
   }
   const backToStep1 = document.getElementById('backToStep1');
   if(backToStep1){
     backToStep1.addEventListener('click',()=>showStep('step-1'));
   }
   const toStep3 = document.getElementById('toStep3');
   if(toStep3){
